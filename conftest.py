# -*- coding: utf-8 -*-
"""
Файл конфигурации Pytest.
Содержит общие фикстуры для всех тестов.
"""

import pytest


# ============================================================================
# ФИКСТУРА С ОБЛАСТЬЮ ДЕЙСТВИЯ function (ПО УМОЛЧАНИЮ)
# ============================================================================

@pytest.fixture
def set_up() -> None:
    """
    Фикстура с областью действия function (по умолчанию).
    Выполняется перед КАЖДЫМ тестом, в который передана.

    Действия до yield: вход в систему
    Действия после yield: выход из системы
    """
    print("\n🔐 [function] Вход в систему выполнен")
    yield
    print("🚪 [function] Выход из системы выполнен")


# ============================================================================
# НОВАЯ ФИКСТУРА С ПАРАМЕТРОМ scope = "module"
# ============================================================================

@pytest.fixture(scope="module")
def set_up_module() -> None:
    """
    Фикстура с областью действия module.
    Выполняется ОДИН РАЗ перед всеми тестами в модуле.

    Действия до yield: подготовка модуля
    Действия после yield: завершение модуля
    """
    print("\n📦 [module] Подготовка модуля (выполняется 1 раз)")
    yield
    print("📦 [module] Завершение модуля (выполняется 1 раз)")