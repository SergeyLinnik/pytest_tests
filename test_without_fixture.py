# -*- coding: utf-8 -*-
"""
Файл с тестами, НЕ использующими фикстуру в качестве аргумента.
Фикстура создана, но не передается в тесты.
"""

import pytest


@pytest.fixture
def set_up() -> None:
    """
    Фикстура создана, но НЕ используется в тестах.
    Содержит yield для демонстрации, но не будет вызвана.

    Действия до yield: вход в систему
    Действия после yield: выход из системы
    """
    # --------------------------------------------------------------------
    # ДЕЙСТВИЯ ДО ЗАПУСКА ТЕСТА (SETUP)
    # --------------------------------------------------------------------
    print("\n🔐 Вход в систему выполнен (фикстура не используется)")

    # --------------------------------------------------------------------
    # ПЕРЕДАЧА УПРАВЛЕНИЯ ТЕСТУ
    # --------------------------------------------------------------------
    yield

    # --------------------------------------------------------------------
    # ДЕЙСТВИЯ ПОСЛЕ ЗАВЕРШЕНИЯ ТЕСТА (TEARDOWN)
    # --------------------------------------------------------------------
    print("🚪 Выход из системы выполнен (фикстура не используется)")


def test_sending_mail_1() -> None:
    """
    Тест отправки письма #1.
    Фикстура НЕ передана в аргументах, поэтому не выполняется.

    Ожидаемый вывод:
    - Письмо #1 отправлено
    """
    print("📧 Письмо #1 отправлено (без фикстуры)")


def test_sending_mail_2() -> None:
    """
    Тест отправки письма #2.
    Фикстура НЕ передана в аргументах, поэтому не выполняется.

    Ожидаемый вывод:
    - Письмо #2 отправлено
    """
    print("📧 Письмо #2 отправлено (без фикстуры)")


# ============================================================================
# ДОПОЛНИТЕЛЬНАЯ ФУНКЦИЯ: СООБЩЕНИЕ ПОСЛЕ ЗАВЕРШЕНИЯ ВСЕХ ТЕСТОВ
# ============================================================================

def pytest_sessionfinish(session: pytest.Session) -> None:
    """
    Функция Pytest, которая выполняется после завершения всех тестов.
    Выводит итоговое сообщение.

    Args:
        session: Объект сессии Pytest
    """
    print("\n" + "="*50)
    print("✅ ВСЕ ТЕСТЫ ЗАВЕРШЕНЫ!")
    print("   Тесты выполнены без использования фикстуры")
    print("="*50)